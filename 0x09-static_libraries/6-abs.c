#include "main.h"

/**
 * _abs - Function that computes the absolute integer value
 * @n: the int to be checked
 * Return: Absolute value of an int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
